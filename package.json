{
  "name": "vue-express",
  "version": "1.0.0",
  "description": "vue express project",
  "author": "jakyoungkim <jakyoung.kim@catenoid.net>",
  "private": true,
  "scripts": {
    "start": "cd ./backend && npm run start",
    "start2": "cd ./frontend && npm install && npm run build && cd ../backend && npm install && npm run start",
    "dev": "cd ./frontend && npm run dev",
    "server": "pm2 start ./backend/bin/www --watch",
    "build": "cd ./frontend && npm run build"
  }
}
